{
  "name": "go-slang",
  "version": "1.0.0",
  "main": "dist/index",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "format": "biome format --write . && biome check --apply .",
    "lint": "biome lint ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.6.0",
    "@types/jest": "^29.5.12",
    "@types/treeify": "^1.0.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "treeify": "^1.1.0"
  }
}
